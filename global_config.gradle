android {

  signingConfigs {
    debug {
      storeFile file('../debug.keystore')
      storePassword 'android'
      keyAlias 'androiddebugkey'
      keyPassword 'android'
    }
    release {
      storeFile file('../wangye.jks')
      storePassword 'kingwild'
      keyAlias 'kingwild'
      keyPassword 'kingwild'
    }

  }

}